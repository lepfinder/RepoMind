# GitHub API Token (optional)
# Set to avoid rate limiting and enable remote commit comparison
# GITHUB_TOKEN=your_github_personal_access_token

# Project scan directory (default: ~/workspace/github)
# REPO_MIND_DIR=/Users/yourname/workspace/github
